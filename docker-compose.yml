services:
  wsp-bot:
    build: .
    restart: unless-stopped
    init: true
    env_file:
      - .env
    environment:
      DATABASE_PATH: /var/lib/wsp/data/wsp.db
    volumes:
      - wsp-data:/var/lib/wsp/data
volumes:
  wsp-data: